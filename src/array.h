#ifndef EUFA_ARRAY
#define EUFA_ARRAY

#include<math.h>
#include<emscripten/emscripten.h>
#include<stack>

using namespace std;

extern "C" {
    extern int add (int x, int y);
    extenn doouble add (double x, double y);
}


#endif